DLSS5 Manager 0.5 Beta10

添加游戏，选择 Core 后点“应用”；完成后再点“启动”。
升级时保留原位置的 data 目录及全部备份。

米哈游只提供 0.4.7 和当前 0.5 Unified5 两个 Core 目标。
游戏有原生 DLSS 时优先原生输入；DX11 配匹配的 Bridge。
需要 Feeder 时选择 0.5 Unified5，0.4.7 不再作为此路线的可用目标。
旧版米哈游安装仍可查看和修改画面配置、修复、恢复或卸载。
普通游戏保留原有历史 Core 选择。

首次应用会检查文件和配套，缺少 NR 显卡运行库时按提示导入。
已有插件先确认备份，取消不会改游戏文件；应用不会自动启动游戏。
画面设置以实际 INI 为准，外部修改优先；不支持的参数不显示。
Core 兼容模式不自动替代 Feeder。0.4.8-beta.3 是旧管理器版本，其 Core 为 0.4.7beta。

本轮只修改管理器；组件未重新编译。实际游戏效果和 RTX20/30 补帧仍需实机验证。

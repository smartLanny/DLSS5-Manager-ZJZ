# 给其他任务：0.5 Core 的 Bridge / Feeder 适配

本任务负责管理器；请在另一个隔离工作树完成 Core 适配，不修改管理器。

1. 从最新累计 0.5 Core 继续，保留已有画质和性能修复，不退回旧 D21 算法。
2. 核对新版 Bridge 配套；参考 `619876` 的外部接入实现补齐 Feeder 接口，处理帧归属、同步与资源释放。先核实该参考提交和当前源码身份。
3. 验证原生、Bridge、Feeder 路线，以及多层、切分辨率、停用与退出。分别记录软件测试、GPU 测试和真实游戏结果。
4. 交回完整首装配套目录、完整 INI 模板及解析规则、源码版本、逐文件大小和 SHA-256、接口能力与兼容说明。OTA 增量不能代替完整配套。

管理器侧将按完整清单接入；收到前保留当前可用内置组件，并把尚未适配的路线明确标为“待适配”。不要求你发布 Release 或替换用户游戏文件。
